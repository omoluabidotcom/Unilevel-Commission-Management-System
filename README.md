# Unilevel Commission System

A Unilevel commission tracking web application.

## Getting started

1. Install dependencies:

```bash
npm install
```

2. Create your environment file:

```bash
copy .env.example .env
```

3. (Optional) If you’re using MySQL, create a database and run the schema in `db/schema.sql`.

4. Run in development mode:

```bash
npm run dev
```

5. Open http://localhost:3000 in your browser.

## Project structure

See the provided template in the repository for the folder layout.
